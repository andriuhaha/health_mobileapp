import 'package:flutter/material.dart';

class NutritionalInputScreen extends StatelessWidget {
  const NutritionalInputScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Enter Nutritional Values')),
      body: const Center(
        child: Text('Feature: Nutritional Analysis Coming Soon...'),
      ),
    );
  }
}
