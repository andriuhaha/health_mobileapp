import 'package:flutter/material.dart';

class UploadLabScreen extends StatelessWidget {
  const UploadLabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Upload Lab Results')),
      body: const Center(
        child: Text('Feature: Lab Results OCR Coming Soon...'),
      ),
    );
  }
}
